# 软著材料生成 Skill

适用场景：根据仓库代码、`information.txt`、说明材料和可运行界面，生成中国软件著作权登记所需的两份核心 PDF：`源代码` 与 `软件设计说明书`。

## 1. 总原则

- 只写经代码、配置、脚本、界面或现有文档验证过的事实。
- 不臆造功能、角色、接口、数据库、截图、图形或页数。
- 先分析，再截图，再制图，再写文档，再导出 PDF，最后清理。
- 截图流程与软件设计图流程必须分开执行，禁止混用一套处理规则。
- 优先使用项目已有包装命令和脚本，禁止默认假设只有某一种技术栈。

## 2. 子流程拆分

### 2.1 `analysis`

负责：

1. 补全 `information.txt`
2. 生成 3~5 个软件名称候选并选择首选名称
3. 提取系统事实、模块、接口、部署方式、冲突项和缺失项
4. 判定项目是 Web、H5、Android、Java 桌面、后端服务还是混合架构

命令选择顺序：

1. `Makefile` / `justfile` / `docker-compose.yml` / `compose.yml` / `*.ps1` / `*.bat` / `*.sh`
2. `gradlew` / `mvnw` / `pom.xml` / `build.gradle` / `settings.gradle` / `AndroidManifest.xml`
3. `package.json` / 锁文件 / `vite.config.*` / `next.config.*`
4. `pyproject.toml` / `requirements.txt` / `manage.py`
5. `.sln` / `.csproj`
6. `go.mod`
7. `Cargo.toml`

要求：

- 记录最终实际使用的启动命令、构建命令、服务命令。
- 如果信息不能可靠判断，统一标记为 `待补充` 或 `需用户确认`。

### 2.2 `runtime-screenshots`

这是“运行命令并截图”的独立环节，只处理真实可运行界面。

适用对象：

- Web / H5 前端页面
- 后台管理页
- 健康检查页、状态页、监控页
- 原生移动端或桌面端的真实运行界面

执行要求：

1. 先启动所有必需服务，再截图。
2. Web / H5 界面必须优先使用可用 MCP 浏览器能力进行打开和截图。
3. Android 原生项目优先使用原生截图方式；只有存在真实 WebView/H5 页面时才用浏览器 MCP。
4. 纯后端或无 UI 项目不得伪造页面截图。

覆盖范围：

- 首页
- 主导航页
- 核心业务页
- 数据展示页
- 监控页
- 图表页
- 地图页 / 三维页
- 健康检查页（若有展示价值）

截图规则：

- 截图以“清晰、真实、主体完整”为第一原则。
- 不得对截图套用“系统图满页铺开”的规则。
- 不得为了消除空白而裁掉关键 UI。
- 可以裁掉纯背景、浏览器外边距、无意义留白，但前提是不影响主体。
- 能裁时，四周留白尽量控制在 `20px` 以内；不能裁时保留真实布局。
- 插入文档时，截图应按清晰度优先，避免被强行拉伸到失真。

说明文字要求：

- 每张截图必须有独立说明。
- 每张截图说明不少于 `200` 字。
- 说明必须包含：页面名称、访问入口、主要功能、关键区域、与其他模块关系、纳入软著材料的意义。

目录要求：

- 临时目录：`.tmp/softdocs/screenshots/`
- 最终目录：`docs/assets/screenshots/`

### 2.3 `code-diagrams`

这是“根据代码和材料生成软件设计图”的独立环节，不依赖截图流程。

必须生成：

1. 用例图
2. 系统架构图
3. 部署图
4. 业务流程图
5. 详细流程图 / 时序图 / 状态图
6. E-R 图或数据对象关系图

数据来源：

- 源代码结构
- 组件关系
- 服务调用关系
- 接口与消息流
- 数据对象与字段
- 部署脚本与运行方式
- 已验证说明材料

禁止事项：

- 禁止脱离代码虚构模块关系。
- 禁止把截图当作架构图或流程图替代品。
- 禁止所有图统一套用同一个固定宽高。

生成策略：

1. 先生成图定义（Mermaid / PlantUML / Graphviz 等）。
2. 按图复杂度确定字体、节点间距、画布比例。
3. 先渲染，再检测主体边界，再决定是否重渲染或裁边。
4. 以“字够大、图够满、留白少”为目标迭代。

图形优化规则：

- 字体要偏大，优先保证打印后仍可读。
- 画布大小应根据图形内容自适应，不得固定死为统一长宽。
- 横向流程长图优先横向展开；纵向层级图优先纵向展开。
- 如果节点较少，应缩小画布，避免图只占中间一小块。
- 如果节点较多，应扩大画布并增大节点间距，避免重叠。
- 渲染后四周空白应尽量控制在 `20px` 以内。
- 若超出 `20px`，必须重设画布、缩放或自动裁边后再输出。

质量验收：

- 不允许空白图。
- 不允许图主体缩成页面中间一小块。
- 不允许文字过小到打印后难以辨认。
- 不允许大面积无意义留白。

说明文字要求：

- 每张设计图必须有独立说明。
- 每张设计图说明不少于 `100` 字。
- 说明必须包含：图的用途、主要元素、关系含义、在系统中的作用。

目录要求：

- 临时目录：`.tmp/softdocs/diagrams/`
- 最终目录：`docs/assets/diagrams/`

### 2.4 `docs`

在截图和设计图都验收通过后，才能开始写文档。

必须生成：

- `源代码整理稿.md`
- `软件设计说明书.md`

插图规则：

- 图片标题必须位于图片下方并居中。
- 表格标题必须位于表格上方并居中。
- 每张图片后都必须跟对应说明。
- 图文顺序必须与章节编号一致，不得出现 `4-x` 图片排在 `6-x` 图片之后这类错位。
- 设计图与截图要使用不同的排版策略：
  - 设计图：优先铺满版心，突出结构与文字可读性。
  - 截图：优先保真，不强制拉伸，不得把原本清晰界面放大到模糊。

章节归属：

- 用例图：系统概述 / 需求分析
- 系统架构图、部署图：总体设计
- 流程图、时序图、状态图：详细设计
- E-R 图：数据设计
- 截图：界面设计 / 详细设计 / 接口设计 / 附录

页数密度：

- 设计说明书非最后页每页尽量不少于 `30` 行。
- 源代码稿非最后页每页尽量不少于 `50` 行。

### 2.5 `pdf`

PDF 规则只在 Markdown 定稿后执行。

官方提交要求：

- 必须提交 PDF
- A4 纵向排版
- 连续页码
- 页眉标注“软件名称 + 版本号”
- 单个 PDF 一般不超过 `20MB`

页数规则：

- 源代码 PDF：
  - 超过 `60` 页：只保留前 `30` 页和后 `30` 页
  - 不足 `60` 页：提交全部
- 文档 PDF：
  - 超过 `60` 页：只保留前 `30` 页和后 `30` 页
  - 不足 `60` 页：提交全部

注意：

- 裁剪后的提交稿页码必须重新连续编号，不能沿用原始 `200+` 页页码。
- 页眉页脚必须在重新生成 PDF 时正确预留区域，禁止覆盖正文。
- 源代码 PDF 不得插入图片或截图。
- 可以保留 `*.full.pdf` 作为内部完整备份。

### 2.6 `cleanup`

只有在最终 Markdown、图片、PDF 全部生成并校验通过后才能清理。

保留：

- `docs/assets/diagrams/`
- `docs/assets/screenshots/`
- 最终 Markdown
- 最终 PDF
- `*.full.pdf`

删除：

- 不再被引用的临时图片
- `.tmp/softdocs/`
- 旧的无效中间文件

清理后必须复核：

- Markdown 中图片链接是否仍然有效
- PDF 是否仍能正常打开
- 最终页数、页眉页脚、图题表题是否正确
- 文中图号顺序、附录索引顺序与实际章节位置是否一致
- `docs/assets/diagrams/` 与 `docs/assets/screenshots/` 中是否仍有未被引用的旧文件

## 3. 推荐执行顺序

1. `analysis`
2. `runtime-screenshots`
3. `code-diagrams`
4. `docs`
5. `pdf`
6. `cleanup`

## 4. 最终交付清单

- `information.txt`
- 软件名称候选与首选说明
- 项目关键信息提取说明
- `源代码整理稿.md`
- `软件设计说明书.md`
- `源代码整理稿.pdf`
- `软件设计说明书.pdf`
- 如有超页：对应 `*.full.pdf`
